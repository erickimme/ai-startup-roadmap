# Supabase 프로젝트 설정 → API 에서 복사. .env.local 로 저장하고 Vercel env 에도 추가한다.
# anon key 는 공개되어도 안전(RLS 가 보호). service_role 키는 절대 여기에 넣지 않는다.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
